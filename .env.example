# Copy this file to .env and fill in your values
# cp .env.example .env

# Required: your Anthropic API key
ANTHROPIC_API_KEY=sk-ant-...

# Optional: directory where JSON results are saved (default: results)
OUTPUT_DIR=results

# Optional: max characters of page text sent to Claude (default: 20000)
MAX_CHARS=20000
